import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.salaryexpensetracker.app',
  appName: 'Yearly Salary & Expense Tracker',
  webDir: 'dist'
};

export default config;
